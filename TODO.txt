- the worker code should be extracted from the functional test
-- WrapperTest should use Worker instead of directly the process
-- the Runner should use Worker plus many ExecutableTest
- if a worker crashes, it should be respawned




